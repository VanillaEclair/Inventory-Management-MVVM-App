<UserControl x:Class="Mei.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mei.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Text="Status Message" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 20" FontFamily="Lucida Sans Unicode" />

        <TextBlock Grid.Row="2" Text="Login" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 20" FontFamily="Lucida Sans Unicode" />

        <StackPanel Grid.Row="3">
            <TextBlock Grid.Row="0" Text="Username:" FontSize="12" FontWeight="Normal" HorizontalAlignment="Left" Margin="276 4 0 4" FontFamily="Lucida Sans Unicode" />
            <TextBox Text="{Binding InputUsername, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0 10"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Grid.Row="0" Text="Password:" FontSize="12" FontWeight="Normal" HorizontalAlignment="Left" Margin="276 4 0 4" FontFamily="Lucida Sans Unicode" />
            <TextBox Text="{Binding InputPassword, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0 10"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20">
            <Button 
               FontFamily="Lucida Sans Unicode"
               Content="Login" 
               Width="100" 
               Margin="5"
               Command="{Binding LoginCommand}"/>

        </StackPanel>


    </Grid>
</UserControl>
